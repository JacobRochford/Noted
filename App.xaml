<Application x:Class="Noted.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Noted"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <SolidColorBrush x:Key="NotedWindowBackgroundBrush" Color="#F5F9FC"/>
        <SolidColorBrush x:Key="NotedSurfaceBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="NotedSubtleSurfaceBrush" Color="#F2F8FB"/>
        <SolidColorBrush x:Key="NotedInputBackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="NotedBorderBrush" Color="#C9DFEC"/>
        <SolidColorBrush x:Key="NotedStrongBorderBrush" Color="#87CEEB"/>
        <SolidColorBrush x:Key="NotedTextBrush" Color="#355A6E"/>
        <SolidColorBrush x:Key="NotedSecondaryTextBrush" Color="#78909C"/>
        <SolidColorBrush x:Key="NotedMutedTextBrush" Color="#8BA5B2"/>
        <SolidColorBrush x:Key="NotedAccentBrush" Color="#5BA8C8"/>
        <SolidColorBrush x:Key="NotedAccentHoverBrush" Color="#4A97B7"/>
        <SolidColorBrush x:Key="NotedAccentPressedBrush" Color="#3E839F"/>
        <SolidColorBrush x:Key="NotedAccentSoftBrush" Color="#DCEEF6"/>
        <SolidColorBrush x:Key="NotedAccentSoftHoverBrush" Color="#C9E4F0"/>
        <SolidColorBrush x:Key="NotedAccentTextBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="NotedDangerBrush" Color="#B84F4F"/>
        <SolidColorBrush x:Key="NotedWarningBrush" Color="#B8761F"/>
        <SolidColorBrush x:Key="NotedSuccessBrush" Color="#347052"/>

        <Style x:Key="NotedContextMenuItem" TargetType="MenuItem">
            <Setter Property="Foreground" Value="{DynamicResource NotedTextBrush}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="ItemBackground"
                                Margin="2"
                                Padding="{TemplateBinding Padding}"
                                Background="Transparent"
                                CornerRadius="5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="22"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  ContentSource="Icon"/>
                                <ContentPresenter Grid.Column="1"
                                                  VerticalAlignment="Center"
                                                  ContentSource="Header"
                                                  RecognizesAccessKey="True"/>
                                <TextBlock Grid.Column="2"
                                           Margin="18,0,0,0"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource NotedSecondaryTextBrush}"
                                           FontSize="11"
                                           Text="{TemplateBinding InputGestureText}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBackground" Property="Background" Value="{DynamicResource NotedAccentSoftBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ItemBackground" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NotedContextMenu" TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource NotedWindowBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource NotedBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="3"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="12"
                                                  ShadowDepth="3"
                                                  Opacity="0.18"
                                                  Color="#000000"/>
                            </Border.Effect>
                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
