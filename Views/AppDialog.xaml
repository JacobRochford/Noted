<Window x:Class="Noted.AppDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Noted"
        Width="380"
        MinHeight="160"
        MaxHeight="540"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        Background="Transparent"
        Foreground="{DynamicResource NotedTextBrush}"
        FontFamily="Segoe UI">
    <Window.Resources>
        <Style x:Key="DialogButton"
               TargetType="Button"
               BasedOn="{StaticResource NotedSecondaryButton}">
            <Setter Property="MinWidth" Value="84"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Padding" Value="14,4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="PrimaryDialogButton"
               TargetType="Button"
               BasedOn="{StaticResource NotedPrimaryButton}">
            <Setter Property="MinWidth" Value="84"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Padding" Value="14,4"/>
        </Style>
    </Window.Resources>

    <Grid Background="Transparent">
        <Border Margin="8"
                Background="{DynamicResource NotedWindowBackgroundBrush}"
                BorderBrush="{DynamicResource NotedStrongBorderBrush}"
                BorderThickness="1"
                CornerRadius="9">
            <Border.Effect>
                <DropShadowEffect BlurRadius="14"
                                  ShadowDepth="3"
                                  Opacity="0.2"
                                  Color="#000000"/>
            </Border.Effect>
            <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Width="34"
                        Height="34"
                        Margin="0,0,14,0"
                        VerticalAlignment="Top"
                        Background="{DynamicResource NotedAccentSoftBrush}"
                        BorderBrush="{DynamicResource NotedAccentSoftHoverBrush}"
                        BorderThickness="1"
                        CornerRadius="17">
                    <TextBlock x:Name="IconText"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource NotedAccentBrush}"
                               FontSize="20"
                               FontWeight="Bold"
                               Text="i"/>
                </Border>

                <StackPanel Grid.Column="1">
                    <TextBlock x:Name="TitleText"
                               Margin="0,1,0,8"
                               Foreground="{DynamicResource NotedTextBrush}"
                               FontSize="17"
                               FontWeight="SemiBold"
                               TextWrapping="Wrap"/>
                    <ScrollViewer MaxHeight="390"
                                  VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="MessageText"
                                   Foreground="{DynamicResource NotedTextBrush}"
                                   FontSize="13"
                                   LineHeight="20"
                                   TextWrapping="Wrap"/>
                    </ScrollViewer>
                </StackPanel>
            </Grid>

            <CheckBox x:Name="SuppressionCheckBox"
                      Grid.Row="1"
                      Margin="48,14,0,0"
                      Style="{StaticResource NotedCheckBox}"
                      FontSize="12"
                      Visibility="Collapsed"/>

            <Border Grid.Row="2"
                    Height="1"
                    Margin="0,16,0,12"
                    Background="{DynamicResource NotedBorderBrush}"/>

            <StackPanel x:Name="ButtonsPanel"
                        Grid.Row="3"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal"/>
            </Grid>
        </Border>
    </Grid>
</Window>
