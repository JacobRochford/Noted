<Window x:Class="MyNotes.NewNoteNameDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Create Note"
    Width="380"
    Height="210"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize"
    WindowStyle="SingleBorderWindow"
    Background="#FAFAFA"
    FontFamily="Segoe UI">
    <Border Margin="0" CornerRadius="10" Background="#FAFAFA">
        <Border.Effect>
            <DropShadowEffect BlurRadius="14" Direction="270" Opacity="0.18" ShadowDepth="4" Color="#000000"/>
        </Border.Effect>
    <Grid Margin="24">
        <Grid.Resources>
            <!-- Rounded TextBox Style -->
            <Style x:Key="RoundedTextBox" TargetType="TextBox">
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="BorderBrush" Value="#BFD7EA"/>
                <Setter Property="BorderThickness" Value="1.5"/>
                <Setter Property="Background" Value="White"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Padding" Value="8,5"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="Bd" CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <!-- Rounded Button Style -->
            <Style x:Key="RoundedButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="MinWidth" Value="84"/>
                <Setter Property="Padding" Value="12,6"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#E5EAF1"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#D1D5DB"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Enter note title"
               FontSize="18"
               FontWeight="Bold"
               Foreground="#1A3A4A"
               Margin="0,0,0,16"/>

        <TextBox x:Name="NoteNameTextBox"
             Grid.Row="1"
             MinWidth="260"
             Margin="0,0,0,4"
             KeyDown="NoteNameTextBox_KeyDown"
             Style="{StaticResource RoundedTextBox}"/>

        <StackPanel Grid.Row="3"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Margin="0,12,0,0"
                MinWidth="200">
            <Button Content="Cancel"
                    Click="CancelButton_Click"
                    Margin="0,0,10,0"
                    Background="#F3F6F9"
                    Foreground="#1A3A4A"
                    BorderBrush="#D1D5DB"
                    BorderThickness="1"
                    Style="{StaticResource RoundedButton}"/>
            <Button Content="Create"
                    Click="CreateButton_Click"
                    Background="#2C6E91"
                    Foreground="White"
                    BorderBrush="#2C6E91"
                    BorderThickness="1"
                    Style="{StaticResource RoundedButton}"/>
        </StackPanel>
    </Grid>
    </Border>
</Window>